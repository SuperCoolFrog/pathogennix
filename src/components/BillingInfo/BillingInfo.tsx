import React from 'react';
import styles from './billing-info.module.scss';

const ReviewCart = () => {
  return (<section className={styles.billingInfoContainer}>
    <header className={styles.header}>
      <h2>Billing Info</h2>
    </header>
    <div className={"pure-g"}>
    </div>
  </section>)
};

export default ReviewCart;